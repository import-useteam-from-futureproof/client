import React from 'react';
import { Chatroom } from '../../components';
import { QuizController } from '../../layout';

export default () => {
	return (
		<>
			<Chatroom />;
			<QuizController />;
		</>
	);
};
